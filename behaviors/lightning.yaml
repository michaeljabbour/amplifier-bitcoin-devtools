bundle:
  name: bitcoin-devtools-lightning
  version: 0.1.0
  description: >
    LND Lightning Network tools — invoices, payments, channel balances,
    and node info via the LND REST API.

tools:
  - module: tool-lnd
    source: ../modules/tool-lnd
    # Config resolved from env: LND_REST_HOST, LND_REST_PORT, LND_TLS_CERT, LND_MACAROON_PATH

agents:
  include:
    - bitcoin-devtools:lightning-specialist

context:
  include:
    - bitcoin-devtools:context/instructions.md
